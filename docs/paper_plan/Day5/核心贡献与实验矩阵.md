# Day 5 完结：核心贡献锁定与实验矩阵

日期 2026-06-11。微观抽查和真值核验都支持方向可行，按计划把核心贡献正式锁定，并排出完整的实验矩阵。本文是 Day 6 大纲的直接输入。

## 锁定的两条核心贡献

第一条，内部反馈驱动的检测失效发现与结构定位一体化框架。差分 oracle 不依赖真值标注，靠跨模型一致性发现四类失效；结构归因用一次梯度×激活反传，把每个失效实例定位到结构位置——backbone 的哪一阶段、FPN 的哪个尺度、head 的哪条分支、特征图的哪片空间区域；观察式归因与干预式层级消融互相印证。多维反馈的 fuzzing 机制是这个框架的失效发现引擎，不单列成贡献。框架对输入源无关：自然差分分歧、fuzzing 生成、腐蚀诱发的失效都走同一条归因管线。

第二条，跨架构的结构失效签名。三种代表性架构各自有稳定且可解释的结构失效签名：faster_rcnn 的虚检集中在它独有的最细尺度 P2；retinanet 的漏检指向 FPN 融合层（fpn.inner 和 fpn.layer 的份额比值 2.5 到 3.3 倍）；fcos 的虚检集中在 P3，定位偏移指向回归分支（head.reg 比值约 3.5 倍）。证据有三重：观察式归因、干预式消融、真值清洗后签名不变（见同目录真值核验文档）。这是"检测模型的缺陷在结构上集中在哪里"的第一份系统证据，对应 Day 1 钉死的贡献声明。

只锁这两条。其余产出（检测差分 oracle 的真值核验方法、漏检的低分候选归因）都当作方法细节放进对应小节，不抬成贡献。

## 实验矩阵

固定设置：模型集合是 torchvision 三件套（Faster R-CNN、RetinaNet、FCOS，两阶段/一阶段、anchor-based/anchor-free 都覆盖）；数据集是 COCO val2017；阈值 score 0.5、聚簇 IoU 0.5、loc 0.7。E3 专门检验阈值这个设定，E9 扩模型集合。

| 编号 | 实验 | 目的 | 关键指标 | 前置 | 状态 |
|---|---|---|---|---|---|
| E1 | 全量结构签名 | 主实验：三模型轮换目标，四类失效的归因画像与责任尺度 | 失效计数、逐桶份额相对 agree 的比值、责任尺度分布、落框率 | 无 | 完成（06-12，全量标定定参 t_cov=0.85 后重跑） |
| E2 | 真值核验 | oracle 判定相对真值的混淆表 + 签名真值不变性 | 各类确认率、确认子集上的签名 | E1 | 完成，随 E1 每次运行自动产出 |
| E3 | 校准阈值对照 | 排除"签名由分数标定差异主导"的替代解释 | 每模型校准到同一工作点后重算 E1 指标 | E1 | 未做 |
| E4 | 层级消融 | 干预式对照证据 | FPN 层级置零后的失效计数变化矩阵 | E1 | 完成，随 E1 每次运行自动产出 |
| E5 | 反馈循环消融 | 证明多维反馈设计合理 | 单位预算的独特缺陷数、失效多样性 | fuzzing 适配 | 完成（06-13 按独特缺陷复核：base 独特缺陷与真值确认均居四档之首，nofb 触发多而发现少，diff 重复率 30 倍、独特缺陷低于随机） |
| E6 | 自然失效 vs 生成失效 | 看 fuzzing 生成的失效落在自然签名上还是新位置 | 两组失效的结构分布条件比较 | E5 | 完成（06-12，fcos 签名重现、retinanet 放大、frcnn loc 样本不足） |
| E7 | 正面外部验证 | 换诱发手段（COCO-C 风格腐蚀）+ 换裁判（真值），看签名是否收敛 | 给定失效类型的结构分布收敛性 | 无 | 完成（06-13，签名收敛下钻到层级且五档强度稳健：fcos loc 首层三源一致、四档强度居首；可用工作点强度 1~2） |
| E8 | 反面修复验证 | 解冻责任层微调对比同预算非责任层，证因果 | 该类失效削减量、mAP 不退化护栏、双裁判计数 | E7，模式可信 | 未做，排最后 |
| E9 | YOLO 接入 | 架构多样性 + 适配层与按名标签对齐的验证 | E1 指标在四模型设置下复算 | 适配器 | 完成（06-12，四模型复算成立，YOLO 仅黑盒投票） |
| E10 | 随机基线 | 主张一的对照：同 ε 预算无引导扰动 vs 完整框架 | 同预算独特缺陷数、独特缺陷真值确认率、失效多样性 | 无 | 完成（06-13 修正版：独特缺陷 1.7~3.1 倍，真值确认的独特缺陷 2.5~4.2 倍，确认率高 23~27 个百分点） |

报告原则有四条，前三条来自真值核验的教训。差分计数和真值过滤后的计数分开报，不混在一张表里。cls 判定降级使用：两票多数的真值支持率只有 24% 到 48%，模式层面的主张只在真值或更多模型裁决下做。miss 的 consensus_unsupported 子集单独列出，注明 COCO 标注不完备使它是混合裁决，不计入 oracle 错误率。第四条（2026-06-13 增）：触发记录数与独特缺陷数分开报，效率主张用独特缺陷数，触发记录数作为复现难易的次要观测保留。

## 方向修正（2026-06-12）

与作者讨论后修正评估主线。框架的目标是找到目标模型的缺陷,要验证的是两件事:第一,这个框架比同预算的替代手段更能找到目标模型的缺陷;第二,找到的确实是真缺陷。多个模型只是差分裁判的机制需要,不是研究对象;我们不是在寻找多个模型的统一错误模式。

由此修正三点。

第一,贡献二的表述从"跨架构的结构失效签名"调整为"目标模型的结构缺陷定位证据"。三条结论本来说的就是各目标模型自己的缺陷集中位置(faster_rcnn 的虚检在它独有的 P2,retinanet 的漏检在 FPN 融合层,fcos 的定位偏移在回归分支)。跨架构对比在论文里的位置降级为方法判别力检查:不同架构定位到不同位置,说明方法不输出固定偏置,仅此而已,不作为发现本身。

第二,定位粒度下钻。桶级份额保留为可读摘要,正式证据下钻到目标模型内部的具体卷积层:每类失效给出相对 agree 比值最高的 top-k 层,带样本量(实现于 det_analysis 的 layer_drilldown,报表进 det_metrics.md)。通道级证据走覆盖账本(单元=层×尺度×通道)与 OOD 探针,连通 E8 修复验证——解冻的对象从桶细化到层。跨模型对齐只保留在差分裁判需要的标签名层面,不再约束结构分析的粒度。

第三,主张一需要基线对照表,矩阵增补 E10。四档对比:random(同 ε 预算无引导,E10)、corruption(换扰动族无引导,E7)、diff(只有差分梯度,E5 组之一)、base(完整框架),同预算下比较独特缺陷数、独特缺陷的真值确认率、失效多样性。预算定义为变异次数;PGD 步数是方法的超参数,步数与效果预期呈倒 U 形,边界探索不是本期重点。主张二的证据沿用:E2 真值核验、E6 自然 vs 生成、E7 签名收敛。

## 评测与机制的分层（2026-06-13）

与作者讨论缺陷重复计数问题后确立的设计原则。框架分两层:机制层(变异与调度)只看过程信号——覆盖增量、触发数、逐类覆盖缺口、被挑次数、语义有效性,这些信号不预设什么是好的最终产出;评测层在事后计算产出指标——独特缺陷数(按 种子图+类型+位置 聚类去重,对应传统 fuzzing 的 unique crashes)、真值确认率、失效多样性。连接两层的是论文的核心假设:好的过程信号自然导向好的产出。这是待检验的命题,不是设计承诺,所以缺陷身份判定绝不反馈进调度——倒灌会使检验自我实现。传统 fuzzing 的分工与此一致:AFL 的反馈信号是覆盖新颖性,独特崩溃数只出现在最终报告里。

假设的既有依据来自分类器阶段的观察:完整框架的缺陷触发总数低于纯 PGD(PGD 从设计上就朝让模型出错的方向走,触发效率高),但缺陷种类数更高。检测侧这个现象是否重现、相对外部基线(random/corruption)是否成立,由修正指标后的 E5/E10 回答。

循环内的"新失效"判定只对种子的自然基线失效去重(这属于"诱发失效"的定义,不属于按身份调度),跨轮去重只发生在评测端;全部触发记录落盘 detail.json,评测指标可离线重算。

## 与七日计划的衔接

Day 5 的四个框全部完成。Day 6 写大纲时,方法部分按贡献一的管线展开(差分 oracle、结构归因、层级消融、真值核验各一小节),实验部分直接引用上面的矩阵,发现部分以修正后的贡献二为主线:每个目标模型的缺陷定位证据(桶 → 层 → 尺度与落框),跨架构对比作判别力检查。E5 的 fuzzing 适配已完成,E3 与 E8 是大纲之后的工程任务。
