---
orchestrator: A
domain: 实验、写作、可视化、审计
---

## 今日计划（Day8，2026-06-23）

用户已确认：启动实验、可视化、审计三条流；写作挂起，等外部深度研究的核心论文带回。

- 实验（队列 1）：误分类重新归因。两处改动——归因层换到分类子网（head.cls / roi.cls），归因目标从 `scores_g` 概率改成 NMS 前 logit。先实现并本地自检，再同步服务器、nohup 启动 `run_repair_pilot --kind cls`。完成标准是责任通道与随机对照可分；本轮先到"启动并确认进程已起 + 回填日志路径"，分离性分析随 GPU 作业完成后再拉回。GPU 作业已登记 BOARD 队列。
- 可视化（队列 1）：编写汇总脚本，读取新结构 `output/det/<run>/<model>/data/result.json`，产出对照表与曲线。顺手核对 compare_experiments.py / validate_results.py 是否还按旧路径 `<目录>/result.json`。
- 审计（队列 3）：`unit_coverage.py` 账本累加对照 spec，偏差先记 [假设]。
- 写作：挂起，不派发。

## 下次开工起点

- 实验：盯 GPU 作业完成，拉回 cls 归因产物，判定责任通道与随机对照是否可分；可分则升结论（需冷启动审查 worker 复核），不可分则记 [假设] 并排查（是否还有归因层/目标之外的成因）。被 nohup 启动的服务器作业，日志路径见 BOARD GPU 队列与实验.md 记录。
- 可视化：汇总脚本回来后复核数值正确，再排队等实验四类结果做统一表格图。
- 审计：模块 3 回来后做第二确认；下一个模块 4 标定阈值是否被使用。
- 写作：等用户带回外部深度研究核心论文，用 writing-worker 写研究现状第一批正文。

## 等待用户决定

- 暂无。
