---
orchestrator: A
domain: 实验、写作、可视化、审计
---

## 今日计划（Day8，2026-06-23）已完成

- 实验：误分类 logit 归因已落地并跑完两轮。代码改动 `cls_logit_target`（修了审计的 scores_g 偏离）。worker 报"末层 cls_logits 可分、建议升结论"，被我读原始产物 + 退化归一化复核否决：cls 差异是整体检测退化（spurious 爆炸）的连带，控制退化后责任通道不比随机更 cls 特异。结论：误分类难以单层逐通道定位（与 E8 一致）。Day9 误分类方向 settle 为诚实叙述。
- 可视化：汇总脚本 `det_summary.py` + `summarize_det.py` 已合并，仅在合成数据上验证过。
- 审计：模块 3 unit_coverage 完成；并对账模块 2 结论（cls 已改 logit）。
- 写作：挂起。
- 基础设施：按用户要求修了 `sync_lab.ps1`，pull/pull-env 自动定向主检出，永不覆盖工作区 output 符号链接；顺手修复了本轮被覆盖的实验工作区符号链接。

今日提交（均合并到 linux，7 个）：Day8 派发 / viz 汇总脚本 / 审计模块3 / 实验 logit / BOARD rsync坑+GPU队列 / 审计模块2对账 / sync 修复。

## 下次开工起点

- 实验：误分类已 settle（难以单层定位，证据充分但仍是 [假设]）。若要升 [结论]，安排一个冷启动审查 worker 独立复核退化归一化分析（这条会进论文叙述、属重要结论）。之后接队列：loc 定向微调（队列3，Day10）或方差实验（队列4）。GPU 空闲。
- 可视化：等实验产出真实 result.json 后在真数据上复核汇总脚本；四类统一表仍等实验。
- 审计：模块 1-3 完成。下一个模块 4（标定阈值是否被使用）。两条待复核假设：分位边界 `>=` 与分类侧 `>` 不一致、频率/obj_cov 用归一化激活而非 spec 字面 out。
- 写作：仍挂起，等用户带回外部深度研究核心论文，用 writing-worker。
- 写作 workspace 仍在旧头（qqylkqzz，空），下次用前 rebase 到 linux 即可。

## 等待用户决定

- 误分类"难以单层定位"是否安排冷启动审查 worker 升 [结论]，还是先保留为证据充分的 [假设]。
- Day9 优先推进哪条（loc 定向微调 / 方差实验 / 其他）。
