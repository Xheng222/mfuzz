---
stream: 审计
owner: none
status: idle
updated: 2026-06-22
---

## 目标

一个模块一个模块地对照 spec 检查代码，确认框架的行为和定义的公式、算法一致，没有哪里偷懒走了样。范围是整个项目，慢慢做，不用一次查完。

## 已知上下文

- [方法] 逐模块对照 docs/实现方案.md，确认行为符合定义，发现的偏差记成 [假设]，核实后升成 [结论]。
- [方法] 一条结论坐实、且要改代码时，搬进正式的审计记录或 issue，这里只留指针。

## 作业队列与下一步

桌面上的活，可以和其它流同时进行。每次查一个模块，大约一小时一块。

1. det_oracle.py 的 IoU 聚类和四类判定是不是符合定义。
2. struct_attr.py 的 grad×act 是不是和定义的归因公式一致。
3. unit_coverage.py 的账本累加是不是符合 spec 的公式。
4. 标定的阈值是不是真的被用上了。
5. engine/loop.py 和各个 adapter 有没有保持机制和评测的分层。

## 阻塞

无。

## 记录

- 建流：填入按模块切分的检查队列。

## 指针

- spec：docs/实现方案.md
- 代码：mfuzz/differential/det_oracle.py, neurons/struct_attr.py, neurons/unit_coverage.py, engine/loop.py, tasks/
