---
stream: 审计
owner: none
status: idle
updated: 2026-06-22
---

## 目标

按照模块逐一对照 spec 检查代码，确认框架行为与定义中的公式和算法一致。检查范围是整个项目，可以分批完成，不要求一次完成全部内容。在可优化算法的地方执行性能优化，提高框架运行效率。

## 已知上下文

- [方法] 逐模块对照 docs/实现方案.md，确认行为是否符合定义。发现偏差时，先记录为 [假设]；核实后再升级为 [结论]。
- [方法] 一条结论确认成立、且需要修改代码时，转入正式审计记录或 issue。这里仅保留指针。

## 作业队列与下一步

这些任务可以和其他工作流同时进行。每次检查一个模块，预计每个模块约一小时。

1. 检查 det_oracle.py 的 IoU 聚类和四类判定是否符合定义。
2. 检查 struct_attr.py 的 grad×act 是否符合定义中的归因公式。
3. 检查 unit_coverage.py 的账本累加是否符合 spec 中的公式。
4. 检查标定阈值是否确实被使用。
5. 检查 engine/loop.py 和各个 adapter 是否保持机制和评测的分层。

## 阻塞

无。

## 记录

- 建流：填入按模块切分的检查队列。

## 指针

- spec：docs/实现方案.md
- 代码：mfuzz/differential/det_oracle.py, neurons/struct_attr.py, neurons/unit_coverage.py, engine/loop.py, tasks/
