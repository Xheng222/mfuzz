---
stream: 实验
owner: A
status: in-progress
---

## 目标

归因分析需要覆盖检测任务中的四类失效：虚检、漏检、误分类和定位偏移。每一类失效都需要给出责任层级特征、因果验证结论、可用修复办法和修复代价。虚检和漏检已经完成；这条工作流负责补齐误分类和定位偏移。整体验证逻辑见 docs/paper_plan/定位与修复验证框架.md。

## 已知上下文

- [决定] 方法不再限定为 training-free，以产生有效结果为准。
- [方法] A、B 两个数据集不相交，A 选责任层/通道、B 评测，判据用项目的 differential judge、不用 mAP。每一步的证据是"责任层 vs 对照层"的差分，不是绝对修复幅度。
- [结论] 虚检和漏检可以通过抑制责任通道修复，效果明显超过随机对照，因果性成立。已复核。
- [结论] 定位偏移因果性成立（剂量响应 + 对照），但抑制和单步权重调整都没有修复。已复核。
- [决定] cls 与 loc 是一对非存在性失效（loc 几何、cls 类别竞争），单层输出抑制或增强都修不了，统一用只解冻责任子网的定向微调修复（loc 用 IoU 损失、cls 用分类损失），并带非责任层对照。叙述按 2×2。完整逻辑见框架文档。
- [假设]（证据充分）误分类在单层逐通道粒度上定位不住：共享分类 conv 与末层 cls_logits 都不可分，退化归一化复核后责任组每单位退化产出的 cls 不超随机。叙述限定"本方法不能干净定位误分类"，与 E8 一致。证据与逐档数据见框架文档。保留为假设、不另设冷启动审查（负向结果、与 E8 一致、已由调度者独立复核）；cls 最终判定等第三步子网级微调给第二次机会。
- [决定] 误分类归因目标用 NMS 前 logit 尺度（已落地）：struct_attr.py 的 `_make_target` 对 cls 用 `cls_logit_target = log(p)-log1p(-p)`，抵消 sigmoid/softmax 高置信处梯度饱和。依据审计流 scores_g 结论。
- [结论]（已复核）faster_rcnn 的 cls 子网 `roi_heads.box_predictor.cls_score` 是 Linear、不在 conv_layers，逐通道机制够不到；cls 试点只能用 fcos/retinanet 的 `head.classification_head.*`。两人确认。
- [设定] cls 试点协议：A=500/B=300，score 0.5、iou 0.5、loc 0.7。末层 cls_logits 选层与 topk：fcos 91 通道用 topk=8，retinanet 819 通道用 topk=64。
- [设定] 生成失效数据布局（regen 产物，第 2/3 条 repair 加载用）：触发输入是变异后图，存 output/det/regen/<model>/samples/gen/r{round:03d}_{seedstem}.png（fcos 99 张 / retina 107 张，每张承载该轮多条失效实例）；detail.json 的 failures[] 每条带 kind、seed_image（原种子名）、observed（变异后检测 label/score/box）、anchor（跨模型共识的正确框+类别 = 监督目标）、png（=image_ref 触发图相对路径，读侧用 out_dir/png）。变异是 ε 球像素扰动、几何不变，种子真值框对生成图直接适用。aggregate.layer_drilldown 给按层归因，定位用。
- [设定] save_failures=400 上限：n_gen_saved 封顶 400 条失效实例（base/regen 的 save_failures）；若评测集需要更多触发样本，提高该值重跑 regen。只有头 ~400 条失效有落盘触发图（image_ref），其余只在 detail.json 有记录、无图可用于修复。
- [设定] regen_full t_cov=0.95（5000 种子尺度重标；base 的 0.85 是 200 种子标定值，t_freq/critical_tau 保持 0.5/0.5）：四模型初始 CNCov_0 统一回 0.30–0.31、留约七成关键单元给循环增长。覆盖是关键单元上的种子并集、随种子数饱和，所以换种子规模必须用 calibrate_det_tcov 在该规模上重扫 t_cov，不能沿用小规模标定值。扫描数据 output/det/tcov_scan/{model}.json。本会话定：regen_full 覆盖不进论文、覆盖图改由 cov_growth_1000 出（见下 [决定]），所以 0.95 不必为一致性改回 0.85。
- [决定] 覆盖增长图与数据集解耦：regen_full（5000 种子）只当结构层缺陷定位数据集，覆盖随种子数饱和、曲线天生平，其覆盖不进论文。覆盖有效性图另起 cov_growth_1000（1000 种子 @ t_cov=0.85、150 轮、faster_rcnn/retinanet/fcos 三模型，关 GT 核验与层级消融）。全篇 t_cov 统一 0.85。配置 configs/det/cov_growth_1000.toml，产物 output/det/cov_growth_1000/<model>/data/result.json（读 cncov_history 出曲线）。
- [假设]（覆盖曲线形状，faster_rcnn 已实测，三模型版 cov_growth_1000 待确认）覆盖增长曲线受两个旋钮支配：t_cov 定天花板、种子数定起点（也在固定 t_cov 下抬高天花板）。实测固定 t_cov=0.85：200 种子 0.410→0.757、500 种子 0.566→0.873、1000 种子 0.691→0.897、5000 种子 0.90 起步即平；固定 1000 种子：t_cov 0.85→0.691/0.897、0.88→0.500/0.755、0.90→0.361/0.665。要爬过 0.8，t_cov 必须 ≲0.86；在此前提下起点只能靠种子数调。据此选 1000@0.85 当覆盖图（过 0.8 最干净、终点近 0.9），500@0.85 是低起点备选。1000 种子起点预览数据 output/det/tcov_scan/faster_rcnn.json。
- [假设] 生成失效四类分布不均，cls 偏稀。detail.json 全量计数 fcos miss724/spurious844/loc560/cls148、retina miss1398/spurious356/loc261/cls72，存在性（miss+spurious）与 loc 充足、cls 最少；可用修复数据是落盘触发图承载的头 ~400 条。loc 在生成失效上数据够做前沿；cls 稀疏（loc generated 全量 B=33 里 cls 基线仅 10）。
- [假设] 生成失效归因下钻（aggregate.layer_drilldown，ratio=相对 agree 的归因比值）：loc 干净指向回归头——fcos head.regression_head.conv.0 ratio 3.75、conv 塔 0/3/6/9 全抬高（n=89）；retina loc 偏 FPN（regression head 不居首，高 ratio 是小 n 的 fpn 块）。cls 两模型都不干净：分类头不居首、body 层仅略超 1，高 ratio 都是小 n 的 FPN，且 cls 实例稀（分析样本 fcos 29/retina 14）。miss/spurious 偏早期 FPN（fcos miss 近平）。据此 cls 定为难定位难修的负向案例，densify 价值低（多给数据也改不了不可分），不重跑 regen 加密；loc 为干净主线。下面 loc 全量前沿已对 fcos 这条做了第一次确认。
- [决定] 闭环结论按用户意愿保留为一个弱的、接近本质的假设，不安排第二确认、不追求升 [结论]。理由：宁可少声称也不过度声称；小样本试点上，一个稳的弱叙述比一个需要反复重验的强叙述更可取。
- [假设]（弱、本质级，有意不升级）闭环方式有效：先 fuzz 出缺陷、再结构归因定位、再只解冻责任子网做定向微调修复，这条路本身能修缺陷。支撑证据：loc 上 fcos 是干净正例（责任子网 head.regression_head 在 lr0.01 修掉约 40% loc，s30 benefit14/cost2、低代价点 benefit4-6/cost≈0，随机与最低归因对照全 24 格点为 0、几乎不掉 agree），retina 是一致反例（归因偏 FPN、责任头微调修不动、高 lr 反变差），合起来是"修复跟着归因走"。有意保持弱：不声称统计强度、不声称跨类跨模型普遍成立，N 偏小（B 33/36 图）。产物 output/det/repair_finetune/generated_loc_{fcos,retinanet}/data/{result.json,frontier.png}。
- [坑] run_repair_pilot.py 不读 profile 缓存（run_baseline 每次重算 detect），换层换模型无缓存污染。
- [坑] 代码里没有 COCOeval，不要把 mAP 作为判据。
- [坑] profile 缓存 key 不含图片数量、跨实验会复用，用缓存前确认不是旧缓存。

## 作业队列与下一步

GPU 一次只能运行一个作业，作业按照顺序排队。依赖关系写在每条任务之后。

修复线改闭环：把整条定位与修复从"自然分歧失效"切到"生成失效（PGD 变异触发的失效）"，自然分歧那条降级为负基线。

1. ✓ regen 已产出。fcos+retinanet 各跑完 50 轮（fcos 新失效 738、retinanet 575，覆盖到 0.971/0.979），output/det/regen/{fcos,retinanet}/data/{result.json,detail.json} 与 samples/gen 已落、已拉回、已验证。数据布局见"已知上下文"末两条 [设定]。第 2、3 条可开工。
2. ✓ run_repair_finetune.py 已改读生成失效。关键认识：生成失效图上未被 PGD 针对的两个 peer 检测器共识可信（PGD 只打垮目标），differential oracle（judge_image / judge_counts）用的正是 peer 共识，所以失效判定与监督目标（rep_box / cons_label）都自洽给出，无需另存标签——只换数据源即可，训练循环 / loc-cls 损失 / 评测 / 前沿不变。新增 --source {generated,natural}（natural=val2017 自然分歧，留作负基线）与 --gen-root；generated 取 <gen-root>/<target>/samples/gen、按 seed 打散切不相交 A/B（默认 2:1）。本地 ruff 全绿，服务器 loc+cls 小 smoke 端到端跑通。
3. ✓ loc 全量 sweep 跑完并拉回（fcos→retina，三对照 responsible/random/bottom）。fcos 干净正例、retina 一致反例，合起来"修复跟着归因走"。这条按用户意愿保留为弱假设（见上 [决定]/[假设]），不复核、不升级。可选跟进 (a)✓ retina loc 换 FPN 责任结构重跑——无定论（loc 1/12、不与对照分开，见"记录"），retina loc 归为弥散归因负向案例；(b) cls 负向案例的 2×2 叙述（归因不可分且稀，不 densify）。虚检/漏检：免训练抑制那条早先因果结论保留。
4. 全量验证两步走（当前主线，替代盲分小数据；root 盘满已把 output 迁 NAS 符号链接）：
   - 第一步（重跑中）：regen_full 全量生成失效数据集——四模型全目标、全量标定、全 val2017 种子、300 轮、save_failures 不截断。两处 GPU OOM 已代码层根治、t_cov 重标 0.95 修初始覆盖过高（见"记录"末两条）。GPU0、CNCov 已确认回 0.293 并健康增长（轮0→30：0.293→0.403）。明天 pull，先看四模型各失效类别产量，确认 loc/cls 触发图够厚。
   - 第二步：改 run_repair_finetune.py 的 _build_ab_paths 让 A/B 按失效 kind 选取（只取带目标 kind 失效的触发图、集中监督），带容量配平对照、报 loc 框平均 IoU（不只过阈计数），在 fcos/retina 上重做 loc/cls 修复验证。fcos loc 干净正例弱结论保留。
5. （后续）方差区间与跨模型普遍性，待第二步定稿后再排。

下面是切到生成失效之前的旧主线记录，保留作背景，自然分歧那条已降级为负基线：

- 误分类重新归因（自然分歧上为负向结论）：logit 目标已落地；共享 conv.9 与末层 cls_logits 通道级都不可分。
- cls 与 loc 的统一定向微调修复（自然分歧版）：代码已实现、服务器 smoke 与显存检查通过、fcos 全量已挂（见"记录"）。设计稿见 docs/paper_plan/定向微调试点实验设计.md，五个待定项已锁定（第十节）：解冻整子网（单层留 `--single-layer` 变体）、loc 用 `1 - IoU`、cls 同时推正类压错类（只推正类留 `--no-push-down` 变体）、学习率按权重范数归一化、步数档 1/3/10/30 × 学习率档 1e-4/1e-3/1e-2。参数是（失效类, 责任子网, 损失）：loc 用 `head.regression_head` + IoU 损失，cls 用 `head.classification_head` + NMS 前 logit 分类损失。**必须带非责任层对照**（随机层 + 最低归因层）。这套骨架在第 3 条复用、只换数据源。

## 阻塞

无。

## 记录

- 误分类两轮归因跑完：round1 fcos 共享 conv `conv.9`、round1a fcos/retina 末层 `cls_logits`，均带 responsible/random/bottom 对照。代码改动：struct_attr.py 加 `cls_logit_target`（cls 目标改 logit 尺度）。产物 output/det/repair_pilot/{cls,cls_logits_fcos,cls_logits_retina}/data/。
- 调度者第二确认（独立读三份原始 result.json）：否决 worker"末层 cls_logits 可分"的判读——worker 只看 cls/agree、漏掉 spurious 爆炸，退化归一化后责任不超随机。构成 worker 与审查者两个 agent 的复核。逐档数据与完整分析见框架文档。
- 出了定向微调试点实验设计稿 docs/paper_plan/定向微调试点实验设计.md（框架第三步的落地），覆盖 loc/cls 统一协议、责任子网与对照层选取、损失与监督信号、解冻范围与各模型模块路径、步数学习率扫描、A/B 划分、代价度量、判据、能力盘点与结果解读。待调度者审查后派 GPU 作业。
- 实现定向微调修复代码：新增 mfuzz/neurons/finetune.py（机制层）与 scripts/run_repair_finetune.py（编排）。finetune.py 提供 subnet_conv_weights/subnet_target（按模块路径收责任子网或单层的 Conv2d 权重）、set_trainable、weight_norm_scale（学习率按权重范数归一化）、loc_loss（1-IoU）、HeadLogits（torchvision 半前向停在 NMS 前 head 输出）+ recover_cls_logit（cls 失效回找 NMS 前 anchor 行与 logit 向量）、cls_loss（推正类压错类的 BCEWithLogits）、pick_random_control / pick_bottom_control（两条对照层选层）。脚本扫步数×学习率网格、三条配置各扫一遍、落 result.json 与代价收益前沿图。设计稿第十节五个待定项已改写为已锁定。本地验证：ruff check / format 全绿；逻辑级 smoke（合成张量 + 假适配器）通过，覆盖损失形状/数值/梯度方向、cls anchor 回找正误两路、选层与可复现、权重范数归一化、SGD 能 step 与权重可恢复、12 格点网格脚手架与前沿图。loc 端到端打通；cls 回找路径机制已通，需服务器上 COCO + 真权重验证回找命中率。未在本地验证：真实 FCOS/RetinaNet 前向、COCO 失效计数、GPU 多步微调收敛（本地无权重无 COCO、不连服务器、不跑 GPU）。
- fcos 全量扫描挂后台后在 sweep 阶段 CUDA OOM 失败，未跑出结果。launcher 脚本 output/det/repair_finetune/run_fcos.sh（loc→cls 串行），日志 output/det/repair_finetune/run_fcos.log，用 GPU 物理卡 1（CUDA_VISIBLE_DEVICES=1），launcher PID 2772507、loc PID 2772512、cls PID 2202904/2203046。未带 bottom 对照：服务器上找不到任何带 aggregate.layer_drilldown 的 fcos 结果（output/det 下无 base/fcos，全盘 grep layer_drilldown 为空），按预案不传 --drilldown，只跑 responsible+random，bottom 跳过（缺 drilldown 来源）。loc 顺利过 setup 进了 sweep：B 集基线计数 {'agree': 1442, 'miss': 62, 'spurious': 185, 'loc': 182, 'cls': 44}，配置层 responsible=head.regression_head、random=backbone.body.layer2.1.conv1。但 baseline 阶段就耗约 18 分钟（12:10→12:28，A=500/B=300 三检测器全量前向），进 sweep 后第一条 responsible 配置在 A 集训练循环里 OOM：train_one_config 调 gf.run → struct_attr.py:85 forward_graph → fcos 整图前向，单进程累计占用 21.59 GiB 把整张 24G 卡吃满（OOM 报告里的 "GPU 0" 是 CUDA_VISIBLE_DEVICES=1 重映射后的本进程视角，物理卡就是 1，同卡另有 tandongli 三个 636 MiB 小进程）。说明 smoke（20+20 图、steps 1,3）规模下不显存的累积问题，到全量多步多格点会把卡撑爆。属设计稿外的内存累积问题，已停下报告、未改代码。cls 是同一 launcher 的第二条命令、loc OOM 退出后自动接着跑，正卡在自己约 18 分钟的 baseline 阶段，进 sweep 后会同样 OOM；尝试 kill cls 进程被 auto 模式守卫拦下，留其自行 OOM 退出。
- 服务器小 smoke 通过（fcos，A=20/B=20，steps 1,3 × lr 1e-3，不带 drilldown 故只跑 responsible+random）。两条命令均端到端跑完无 traceback，result.json 与 frontier.png 都落地。**未改任何代码**。B 集基线计数正常（agree 106 / loc 4 / cls 3 / spurious 14 / miss 4，非全 0 非 NaN）。loc：各格点 loc/agree 计数合理，random 对照每格点精确回到基线（权重恢复正确）。cls：HeadLogits 半前向（model.head / anchor_generator / box_coder.decode / FCOS 分数公式 sqrt(sigmoid·sigmoid)）全链跑通无 API 不匹配；recover_cls_logit 在真 COCO 上命中，"训练实例累计 n_inst" > 0（responsible s1=2 s3=39，random s1=2 s3=6）。观察：cls responsible s1 单步整子网微调扰动很大（cls 3→23、agree 106→58），s3 回落到 cls=3 agree=66，属真模型行为非代码 bug；random 配 cls 损失 agree 恒 106（小 backbone 扰动不改 NMS 后检测，符合预期）。耗时粗估：20+20 图、2 格点×2 配置约 1 分钟级（loc ~22s 主循环、cls ~38s）；主成本是每步在 A 集重跑前向重判差分（与图数、步数线性）。
- 更正上一条"可放心跑全量"：全量首跑 OOM（损失图跨 A 集累加）→ 已改梯度累加修复。根因经读代码确认：train_one_config 的训练循环把 A 集全部图的 `loc_loss` / `cls_loss` 累加进一个 `step_loss` 张量，整个 A 集遍历完才调一次 `.backward()`，`step_loss` 一直引用着每张图前向（gf.run 的 GraphResult、cls 还有 HeadLogits.run）的完整计算图，等于把 500 张图的前向激活图同时压在显存里，单进程累计 ~21.6 GiB 把 24 GB 卡撑爆；`del g` 无效（step_loss 仍引用 g 的图）。smoke 只 20 图、累加不到 OOM 量，所以没暴露。修复改成梯度累加：每张图算本图全部失效实例的损失之和 `img_loss`，有失效（k>0）就立刻 `img_loss.backward()` 释放本图计算图，用 `n_terms` 累计本步总实例数，遍历完 A 集后把累计梯度按 `n_terms` 归一（`p.grad /= n_terms`）再 `opt.step()`，与原 `(step_loss / n_terms).backward()` 数学等价、显存被限制在单图量级；loc/cls 两支同改，cls 的 cls_logits/boxes_orig/scores 与 g 每图 backward 后 del。run_baseline / judge_counts 本就在 `@torch.no_grad()` 的 detect 下跑、不动。只改了 scripts/run_repair_finetune.py，finetune.py 未动。本地 ruff check/format 全绿；逻辑 smoke（合成张量+假适配器）确认逐图 backward + 梯度归一与"全损失求和再 backward"逐元素等价（单步/多步/含空图各档），方向、step、实例计数均正确。
- 服务器小规模显存检查通过（fcos，A=100/B=50，steps 1,3,10,30 × lr 1e-3，不带 drilldown 只 responsible+random），单独验 loc 与 cls。先 kill 掉上一次 OOM 留下的整条 run_fcos.sh 进程链（loc 已 OOM 自退、cls 半正卡在 baseline 同样会 OOM），释放显存后开跑。结果：两条命令均无 OOM，峰值显存随 A 遍历与步数稳定不增长——loc 进程峰值约 3.7 GiB（含 ~1.9 GiB 同卡他人小进程，扣掉约 1.8 GiB 是本进程），跑满到 s10/s30 仍恒定；cls 进程峰值约 3.8 GiB（多一条 HeadLogits 图，略高于 loc，同样恒定）。两者都远低于 24 GB、不再 OOM。证明修复把显存限制在单图量级、不随 A 集规模或步数累积，可放心跑全量（A=500）。
- fcos 定向微调全量（A=500/B=300，修复后）已挂后台跑起来，健康越过原 OOM 点、进了 sweep。先用 pwsh sync_lab.ps1 push -Apply 把最新代码推到服务器（确认 run_repair_finetune.py 是梯度累加修复版本到位），未 uv sync。挂在物理卡 GPU 2（CUDA_VISIBLE_DEVICES=2，挑它因只有 ~1.9 GiB 他人小进程、无 repair 作业），launcher 脚本 output/det/repair_finetune/run_fcos_full.sh（loc→cls 串行，全默认格点 steps 1,3,10,30 × lr 1e-4,1e-3,1e-2），日志 output/det/repair_finetune/run_fcos_full.log。launcher bash PID 2458268、loc worker PID 2458372。不带 --drilldown，bottom 自动跳过（服务器仍无 base/fcos 的 layer_drilldown，按预案只跑 responsible+random）。健康确认：模型加载、A 集 500/B 集 300 互不相交、B 集基线计数 {'agree': 1442, 'miss': 62, 'spurious': 185, 'loc': 182, 'cls': 44}（与旧首跑一致，确定性）、配置层 responsible=head.regression_head / random=backbone.body.layer2.1.conv1，并已跑出 sweep 头两个格点 `[responsible] s1_lr0.0001: loc=181 agree=1444 (训练实例累计 223)`、`[responsible] s1_lr0.001: loc=181 agree=1442`——越过原来在第一条训练循环里 OOM 的那个坎、无 OutOfMemory。本进程显存稳定在 3710 MiB（单图量级、不随 A/步数爬向 24 GB），与 A=100 检查一致，修复在全量下成立。baseline 阶段本轮约 4 分钟（GPU 2 较空）。当前只确认进 sweep 即返回，未等 loc/cls 跑完。注：A=100 显存检查的两条进程（loc 635828、cls 3427490，各占 GPU 1/3）当时仍在跑自己单 lr 的 sweep、不碰 GPU 2，kill 它们被 auto 守卫拦下，遂换空闲卡避让、不影响全量。预计 loc+cls 全量耗时数小时（每格点主成本是在 A=500 上重跑三检测器前向重判差分，与图数、步数线性，2 配置×4 步档×3 lr=24 格点/支）。过几小时查进度/拉结果：pwsh -File scripts/sync_lab.ps1 pull -Apply。产物落 output/det/repair_finetune/{loc_fcos,cls_fcos}/data/。
- regen 省着版产物再生已挂后台（恢复"生成失效 + 归因"这条修复线被清掉的产物）。配置 configs/det/regen.toml（extends base，标定集从 train2017 全量缩到 3000 张、目标聚焦 fcos+retinanet、faster_rcnn 仍作差分投票者加载，其余沿用 base：max_iterations=50、PGD、差分、归因、生成失效归因都跑）。先 pwsh sync_lab.ps1 -Apply 把 regen.toml 推到服务器、未 uv sync。挂在物理卡 GPU 0（CUDA_VISIBLE_DEVICES=0；四张卡当时都只有 tandongli 的 pia 小进程各约 636 MiB、每卡 ~1.9 GiB，无我方进程，挑了 util 相对最低的卡 0），入口 nohup bash -c 'PYTHONPATH=. uv run python scripts/run_fuzz.py --config configs/det/regen.toml'，日志 output/det/regen/run_regen.log，python 进程 PID 2975624。健康确认：模型加载、3000 张缩标定集生效（缓存键带图片数，新写 det_fcos_train2017_n3000.pt、不命中 n118287 旧缓存）、两遍流式标定（区间统计 + 频率统计各 3000）跑完无 OOM、fcos 构建完成（种子 194、单元 39328、关键 19679），已进 fuzzing 循环并产失效：轮 0 CNCov 0.839 新失效 20、轮 10 CNCov 0.870 新失效 16、轮 20 CNCov 0.913 新失效 7，迭代推进、覆盖上涨、失效持续产出，本进程显存稳定约 2.3 GiB。只确认越过标定进循环在产失效即返回，未等 max_iterations=50 跑完。fcos（标定+循环+分析）约 1 小时才落 output/det/regen/fcos/data/result.json（含生成失效 + aggregate.layer_drilldown），随后跑 retinanet；整体约两小时。过会儿查进度/拉结果：pwsh -File scripts/sync_lab.ps1 pull -Apply（pull 白名单含 output*）。
- regen 跑完并验证：fcos 50 轮新失效 738（覆盖 0.828→0.971、248.6s）、retinanet 50 轮新失效 575（0.828→0.979、261.7s），日志末"全部完成，产物写入 output/det/regen"、进程已退。pwsh sync_lab.ps1 pull -Apply 拉回。独立验证产物结构（调度者第二确认，读 result.json/detail.json 与服务器 samples）：result.json 的 extra 有失效记录 + aggregate.layer_drilldown；detail.json 的 failures[] 带 anchor（共识正确框+类别=监督目标）/observed/seed_image/png；触发图存 samples/gen（fcos 99 / retina 107 PNG，n_gen_saved=400）。修复线第 2、3 条数据齐备，下一步改 run_repair_finetune.py 从 samples/gen 加载触发图、监督用 anchor、评测看触发图缺陷率。
- run_repair_finetune.py 改读生成失效并验证：数据源从 val2017 自然分歧切到 <gen-root>/<target>/samples/gen 触发图（--source generated，--source natural 留作负基线）。本地 ruff check/format 全绿。服务器 GPU3 两条小 smoke（fcos，A=12/B=8，responsible+random，不带 drilldown）端到端跑通：loc smoke B 基线 {miss13,loc4,agree4,spurious4}，1 步弱档 loc 4→4 未动（符合预期）；cls smoke 同一 B 基线 cls=0（无 cls 可修），responsible s1 反而把 cls 抬到 4、agree 4→1，s3 回落 cls=0、agree 仍 1，random 保持 agree=4——印证 cls 子网极敏感 + 该 B 划分 cls 稀疏。产物 output/det/repair_finetune/smoke_gen_{loc,cls}（可删）。由此得 cls 偏稀的发现（见"已知上下文"末条 [假设]），是否提高 save_failures 重跑 regen 加密数据待用户定。
- 读了生成失效归因下钻（无 GPU，服务器 uv run 解析 result.json 的 extra.det.aggregate.layer_drilldown），得 loc/cls/miss/spurious 各自的责任层归因（见"已知上下文" [假设]）：loc 干净指向 head.regression_head（fcos），cls 不可分。据此决定 loc 走主线、cls 当负向案例、不 densify。把 layer_drilldown 抽到 output/det/regen/<model>/data/layer_drilldown.json 供 --drilldown 用（这次带上 bottom 对照）。
- 启动 loc generated 全量 sweep：launcher output/det/repair_finetune/run_loc_gen.sh（fcos→retina 串行，--source generated --drilldown），nohup GPU1（CUDA_VISIBLE_DEVICES=1），日志 run_loc_gen.log。健康确认：A=66/B=33 不相交，B 基线 {loc34,agree59,miss57,spurious57,cls10}，三对照层就位（responsible head.regression_head / random layer2.1.conv1 / bottom layer2.3.conv1）。早期信号（仅 responsible 侧、对照未出，不算结论）：s3_lr0.01 loc 34→28 且 agree 59→60，s1_lr0.01 loc 32 agree 50。等跑完拉回读三条前沿做第二确认。
- loc 全量 sweep 跑完（fcos→retina，ALL DONE，GPU1，约 1h）。拉回 generated_loc_{fcos,retinanet}/data/{result.json,frontier.png}。读数（uv run 解析 result.json，cost=agree 损失、benefit=loc 下降）：fcos 责任 head.regression_head 前沿明显在对照之上——lr0.01 单调 s3 benefit6/cost-1、s30 benefit14/cost2，random/bottom 全 24 格点 benefit≈0；retina 责任头只往下走（s1_lr0.01 benefit-13、高 lr 越修越差），对照≈0，无配置修得动，与 retina loc 归因偏 FPN 一致。frontier.png 两图直观印证（fcos 蓝线高出橙绿一簇、retina 蓝线下行）。结论详见"已知上下文"末条 [假设]。注：fcos 责任高 lr 也会过冲（s10_lr0.01 agree 59→43），低代价正点不依赖激进档。
- retina loc 责任结构按归因换成 FPN 重跑已挂（可选跟进 (a)，做"反例的反证"）。代码最小改动：scripts/run_repair_finetune.py 加 `--responsible <module_path>` 覆盖——非空时用 finetune.subnet_conv_weights(adapter, path, whole_subnet=True) 按模块路径前缀收该子网下全部 Conv2d 权重当责任结构，绕过 subnet_target 按失效类硬选的默认子网，resp_desc 改成该路径进 result.json 与前沿 legend；为空保持原行为。训练循环 / loc-cls 损失 / 评测 / 前沿 / random、bottom 选层均不动。本地 ruff check/format 全绿。先 pwsh sync_lab.ps1 push -Apply 推到服务器、未 uv sync。挂在物理卡 GPU2（CUDA_VISIBLE_DEVICES=2，四卡都被 tandongli 占 ~2.5–5.4 GiB×2 但都留 ~16–19 GiB 余量，挑 util/显存最低的卡 2；卡况已不是任务里写的 636 MiB pia 小进程），launcher 脚本 output/det/repair_finetune/run_loc_retina_fpn.sh，日志 run_loc_retina_fpn.log，launcher bash PID 346836、worker python PID 346840（uv 包装 346837）。命令：--source generated --gen-root output/det/regen --target retinanet --kind loc --responsible backbone.fpn --drilldown 指 retina loc 下钻表，默认网格 steps 1,3,10,30 × lr 1e-4,1e-3,1e-2，--out output/det/repair_finetune/generated_loc_retina_fpn/data（不覆盖旧 retina 反例 generated_loc_retinanet）。健康确认：模型加载、A71/B36 不相交（107 张 retina 触发图按 seed42 打散 2:1）、B 基线 {loc12,miss123,spurious27,cls6,agree26}（非全 0 非 NaN）、三对照层就位（responsible=backbone.fpn 收到 8 个 FPN conv 权重张量：inner/layer_blocks 0/1/2 + extra p6/p7；random=backbone.body.layer2.1.conv1；bottom=backbone.body.layer3.4.conv2，random/bottom 均 body 层、不与 FPN 重叠）、越过 baseline 进 sweep 无 OOM（本进程显存稳定 3766 MiB 单图量级）、已跑出头几个 responsible 格点（s1/s3/s10 低中档 loc 仍 12 未动、agree 26→26/27/28 微动，高 lr 高步档在后）。只确认健康、未等约 1 小时全量跑完，过会儿 pwsh sync_lab.ps1 pull -Apply 拉回读 generated_loc_retina_fpn/data/{result.json,frontier.png}。
- retina-FPN 重跑跑完、读数无定论：B 基线 loc12，responsible(backbone.fpn) 最好 s10/s30_lr0.01 loc12→11（benefit 1）且 agree 26→31，但 bottom 对照也到 loc11、random 平——不与对照分开。读 result.json 全类计数：responsible s30_lr0.01 的 agree+5 几乎全来自 miss 123→116（−7）、loc 只动 1，FPN 微调 realized 效果在 miss 不在 loc；bottom（深 body 层）miss 也减 8，说明硬调大结构广泛降 miss、非 FPN 特有。结论：无定论、非方法证伪。retina loc 与 cls 并列为弥散归因负向案例（弱叙述）。
- 反思定位到实验设置/数据问题（用户提出，读代码确认）：修 loc 要用触发 loc 的数据。_build_ab_paths 对全部触发图盲分 A/B、不按 kind 选；训练损失虽只在 loc 失效实例上算（_failure_indices 按 rec.kind 过滤），但 A 集被 miss 主导、带 loc 失效的图是少数，loc 梯度稀薄；FPN 又是共享上游，薄 loc 梯度外溢到 miss。fcos 修得动是因 loc 数据厚（B loc34）+ 回归头是 loc 专属终端结构；retina 两者都不占。其余设置缺陷：loc 评测数据薄（B loc12，±1 噪声地板）、对照未按容量配平（bottom 是深 body 层、硬调也广泛改善）、loc 判据是 IoU<0.7 离散阈值（连续改善不过阈不计）。
- output 迁到 NAS（root 盘 100% 满、仅剩 1.9G，443G 是他人占用、非我方）：服务器 output 改成符号链接 → /home/nas511/dongyajie/mfuzz_output（nas511 9.7T 可写）。cp -a 复制 461 文件校验一致后换符号链接、smoke 验证管线读写正常、删原备份。配置仍写 out=output/det/...，经符号链接落 NAS。sync_lab.ps1 pull 加 --copy-dirlinks 才能跟随符号链接拉回（dry-run 验证：修复前 total 0、修复后见全部产物、samples/ 仍排除）。
- YOLO 可作 fuzz 目标（不只投票者，纠正旧认识）：YoloDetector 有 forward_graph（可微 letterbox+解码支持 PGD）、conv_layers/yolo_bucket/_YOLO_IDX_BUCKETS 结构归因、fpn_info、level_zero_hook 消融；configs/det/yolo_target.toml 早有此用法、yolo11n n118287 标定缓存已在。yolo11n-as-target smoke 端到端跑通（8265 单元、4 轮 38 失效、share_ratio/ablation/gt 全出）。AGENTS.md 旧"YOLO 黑盒投票者"说明已改正。
- 全量数据生成 regen_full 已挂（两步走第一步）：configs/det/regen_full.toml——四检测器全目标轮换、全量标定 profile_images=0（四模型 n118287 缓存全命中、瞬时）、全 val2017 种子 num_images=5000、300 轮、seeds_per_round=8、pool_capacity=1024、save_failures=50000 不截断、gen_attr_per_kind=200。GPU2，launcher PID 371534、worker 371538，日志 output/det/regen_full/run_regen_full.log（注：日志块缓冲，config 行后要到"构建完成"才刷新；用进程状态+result.json 判进度）。健康确认：4 模型加载（GPU2 8.4GiB）、标定缓存命中（worker 读 val2017 做种子基线、不读 train2017，train2017 计数 118287 与缓存键一致）、第一个模型在 5000 种子基线阶段。产物经 output 符号链接落 NAS。预计数小时~十几小时跑完四模型，明天 pull。
- regen_full 首跑 OOM、加 expandable_segments 修复后重挂：首跑（PID 371538）在 5000 种子基线阶段 CUDA OOM——本进程显存 8.4→17.5 GiB，根因是变异/种子图尺寸不一、5000 张过 run_baseline（det_analysis.py:82）时 CUDA 缓存分配器跨尺寸不能复用块、reserved 碎片化累积（OOM 报告自带 expandable_segments 提示）。非泄漏（run_baseline 逐图 no_grad、Detection 存 CPU、Seed 不存图）。修复：PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True 重挂（num_images=5000 不动、无代码改动）+ python -u。重挂 GPU2、launcher PID 376473 / worker 376477，日志 output/det/regen_full/run_regen_full.log（首跑 OOM 日志存 run_regen_full.oom.log）。验证：显存稳定约 9.9 GiB（含他人 ~4.7 GiB、本进程约 5 GiB）、6 分钟 9742→9964 几乎不涨、越过原 OOM 点（~10 分钟）无碍。注：loguru 日志仍像有缓冲，用进程状态+result.json 判进度。明天 pull 前先确认四个 result.json 落地。
- regen_full 全量期间又出两处 GPU OOM、均代码层根治（独立复核 worker 第二确认是仅有的非流式/碎片化通道、修复到位）：(1) 初始覆盖遍历 loop.py:105 的 make_batches 一次性构建全部种子批、把约 5000 张图同时压进显存（本进程 15+GiB），改惰性生成器逐图产出（adapter 协议返回 list→Iterable，两调用点都单遍迭代、安全）；(2) run_baseline 等变长 COCO 图过 CUDA 缓存分配器碎片化（非泄漏、本就逐图流式），把 expandable_segments 从启动器环境下沉到 run_fuzz 的 import torch 前 setdefault、并给 ablate_levels 补每图 del+每层级 empty_cache，使防护与入口无关。配套写稳健启动器 scripts/run_lab_experiment.py：选已用显存最低的空闲卡 + 设 expandable + 失败重选卡重试 + run_fuzz 对已落 result.json 的目标续跑。复核另指两个非阻塞项（不改）：续跑粒度是整目标（analyze 中途崩会重跑整目标含 baseline 与 300 轮）、选卡有竞态靠重试兜底。提交 yxqpwyzu（make_batches+启动器）、xvkoloyr（下沉+ablate）。
- 初始覆盖率过高已修并重跑：旧 t_cov=0.85 是 num_images=200 时标定（CNCov_0≈0.41），覆盖是关键单元上的种子并集、随种子数饱和，5000 种子下首跑 CNCov_0=0.90、Δ 掉到 0.0002、循环无增长空间。用 calibrate_det_tcov 在 5000 种子上重扫三维网格（t_freq×critical_tau×t_cov，复用 n118287 profile、不重标定）：t_cov 是唯一有量程把 0.90 压到 0.3–0.4 且不动关键集的旋钮（tau 改关键集大小与语义、t_freq 改成员、二者是有据的关键度设计，保持 0.5/0.5）；四模型在每档 t_cov 上一致（spread≤0.013）。定 t_cov=0.95（regen_full [coverage] 覆盖，提交 a46a644d），清旧产物经启动器 GPU0 重跑。CNCov 修复已确认：faster_rcnn CNCov_0=0.293（4909 共识种子；scan 用 5000 原始种子预测 0.302，差在种子数）、循环健康增长 0.293→0.301→0.322→0.368→0.403（轮0→30）、λ2 自适应 0.5→2.0、每轮仍产 33–45 新失效。扫描数据 output/det/tcov_scan/{model}.json。
- 覆盖增长图调参（本会话）：用户要一条从初始值增长到 0.8 以上的覆盖曲线。在 faster_rcnn 上扫种子数（200/500/1000/5000 @ t_cov=0.85）与 1000 种子上扫 t_cov（0.85/0.88/0.90），得"t_cov 定天花板、种子数定起点"的关系（数据见上 [假设]）。pilot 配置 configs/det/pilot_cov{200,500,1000,2000,1000_t88,1000_t90}.toml，跑完循环即掐、不进分析（analyze 的 run_attribution 无 stage 开关、是之前拖慢的来源）。calibrate_det_tcov 事后只给起点预览，曲线天花板靠实跑。五条曲线比较图 output/det/cov_curve_compare.png（带 0.8 参考线）。用户选 1000@0.85，据此起正式三模型 cov_growth_1000（GPU2、PID 3326717）。清理了被取代的旧全量实验 output/det/regen（139M、2026-06-24、50 轮旧配置）；pilot_cov* 五个试点与 base/ 暂留，待用户定是否清。

## 指针

- 框架：docs/paper_plan/定位与修复验证框架.md（验证逻辑、四类状态、cls 负向证据）
- 设计稿：docs/paper_plan/定向微调试点实验设计.md（框架第三步落地：loc/cls 定向微调试点的责任子网、对照、损失、协议、判据、能力盘点）
- 代码：mfuzz/neurons/struct_attr.py, repair.py, finetune.py
- 脚本：scripts/run_repair_pilot.py, run_repair_grad.py, run_repair_finetune.py
- 文档：docs/paper_plan/Day6/E8修复验证试点.md
- 产物：output/det/repair_pilot, output/det/repair_grad
- 服务器：实验只在实验服务器运行，地址见项目 CLAUDE.md 的 Lab server 一节
