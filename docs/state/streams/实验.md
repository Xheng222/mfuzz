---
stream: 实验
owner: A
status: in-progress
---

## 目标

归因分析需要覆盖检测任务中的四类失效：虚检、漏检、误分类和定位偏移。每一类失效都需要给出责任层级特征、因果验证结论、可用修复办法和修复代价。虚检和漏检已经完成；这条工作流负责补齐误分类和定位偏移。整体验证逻辑见 docs/paper_plan/定位与修复验证框架.md。

## 已知上下文

- [决定] 方法不再限定为 training-free，以产生有效结果为准。
- [方法] A、B 两个数据集不相交，A 选责任层/通道、B 评测，判据用项目的 differential judge、不用 mAP。每一步的证据是"责任层 vs 对照层"的差分，不是绝对修复幅度。
- [结论] 虚检和漏检可以通过抑制责任通道修复，效果明显超过随机对照，因果性成立。已复核。
- [结论] 定位偏移因果性成立（剂量响应 + 对照），但抑制和单步权重调整都没有修复。已复核。
- [决定] cls 与 loc 是一对非存在性失效（loc 几何、cls 类别竞争），单层输出抑制或增强都修不了，统一用只解冻责任子网的定向微调修复（loc 用 IoU 损失、cls 用分类损失），并带非责任层对照。叙述按 2×2。完整逻辑见框架文档。
- [假设]（证据充分）误分类在单层逐通道粒度上定位不住：共享分类 conv 与末层 cls_logits 都不可分，退化归一化复核后责任组每单位退化产出的 cls 不超随机。叙述限定"本方法不能干净定位误分类"，与 E8 一致。证据与逐档数据见框架文档。保留为假设、不另设冷启动审查（负向结果、与 E8 一致、已由调度者独立复核）；cls 最终判定等第三步子网级微调给第二次机会。
- [决定] 误分类归因目标用 NMS 前 logit 尺度（已落地）：struct_attr.py 的 `_make_target` 对 cls 用 `cls_logit_target = log(p)-log1p(-p)`，抵消 sigmoid/softmax 高置信处梯度饱和。依据审计流 scores_g 结论。
- [结论]（已复核）faster_rcnn 的 cls 子网 `roi_heads.box_predictor.cls_score` 是 Linear、不在 conv_layers，逐通道机制够不到；cls 试点只能用 fcos/retinanet 的 `head.classification_head.*`。两人确认。
- [设定] cls 试点协议：A=500/B=300，score 0.5、iou 0.5、loc 0.7。末层 cls_logits 选层与 topk：fcos 91 通道用 topk=8，retinanet 819 通道用 topk=64。
- [坑] run_repair_pilot.py 不读 profile 缓存（run_baseline 每次重算 detect），换层换模型无缓存污染。
- [坑] 代码里没有 COCOeval，不要把 mAP 作为判据。
- [坑] profile 缓存 key 不含图片数量、跨实验会复用，用缓存前确认不是旧缓存。

## 作业队列与下一步

GPU 一次只能运行一个作业，作业按照顺序排队。依赖关系写在每条任务之后。

修复线改闭环：把整条定位与修复从"自然分歧失效"切到"生成失效（PGD 变异触发的失效）"，自然分歧那条降级为负基线。

1. 等 regen 产出（当前在跑）。等 output/det/regen/{fcos,retinanet}/data/result.json 落地，里面有生成失效（PGD 触发的四类失效及变异前后检测）与 aggregate.layer_drilldown（按层归因）。fcos 约 1 小时、retinanet 再约 1 小时，整体约两小时。产物到位后第 2、3 条才能开工。挂起细节见"记录"末行（GPU 0、PID 2975624、日志 output/det/regen/run_regen.log）。
2. 把 run_repair_finetune.py 改成从生成失效加载触发输入：监督目标用变异前的正确检测（不是 GT，也不是自然分歧的差分共识），评测看触发输入上的缺陷率。依赖第 1 条的 result.json 给出生成失效与变异前检测。
3. 在生成失效上重做四类（虚检、漏检、误分类、定位偏移）的定位与修复。复用已有的责任子网 / 对照层 / 损失 / 协议骨架（设计稿 docs/paper_plan/定向微调试点实验设计.md），但数据换成生成失效；原来在自然分歧上的结论（虚检漏检可抑制修复、loc 因果成立但修不动、cls 通道级定位不住）降级为负基线，新主线结论以生成失效为准。依赖第 2 条改完加载逻辑。
4. 对 A、B 数据集多次划分算方差、给出可报告区间。依赖第 3 条全部点结果定稿。
5. 将修复结论迁移到其他模型，确认是否普遍成立。

下面是切到生成失效之前的旧主线记录，保留作背景，自然分歧那条已降级为负基线：

- 误分类重新归因（自然分歧上为负向结论）：logit 目标已落地；共享 conv.9 与末层 cls_logits 通道级都不可分。
- cls 与 loc 的统一定向微调修复（自然分歧版）：代码已实现、服务器 smoke 与显存检查通过、fcos 全量已挂（见"记录"）。设计稿见 docs/paper_plan/定向微调试点实验设计.md，五个待定项已锁定（第十节）：解冻整子网（单层留 `--single-layer` 变体）、loc 用 `1 - IoU`、cls 同时推正类压错类（只推正类留 `--no-push-down` 变体）、学习率按权重范数归一化、步数档 1/3/10/30 × 学习率档 1e-4/1e-3/1e-2。参数是（失效类, 责任子网, 损失）：loc 用 `head.regression_head` + IoU 损失，cls 用 `head.classification_head` + NMS 前 logit 分类损失。**必须带非责任层对照**（随机层 + 最低归因层）。这套骨架在第 3 条复用、只换数据源。

## 阻塞

无。

## 记录

- 误分类两轮归因跑完：round1 fcos 共享 conv `conv.9`、round1a fcos/retina 末层 `cls_logits`，均带 responsible/random/bottom 对照。代码改动：struct_attr.py 加 `cls_logit_target`（cls 目标改 logit 尺度）。产物 output/det/repair_pilot/{cls,cls_logits_fcos,cls_logits_retina}/data/。
- 调度者第二确认（独立读三份原始 result.json）：否决 worker"末层 cls_logits 可分"的判读——worker 只看 cls/agree、漏掉 spurious 爆炸，退化归一化后责任不超随机。构成 worker 与审查者两个 agent 的复核。逐档数据与完整分析见框架文档。
- 出了定向微调试点实验设计稿 docs/paper_plan/定向微调试点实验设计.md（框架第三步的落地），覆盖 loc/cls 统一协议、责任子网与对照层选取、损失与监督信号、解冻范围与各模型模块路径、步数学习率扫描、A/B 划分、代价度量、判据、能力盘点与结果解读。待调度者审查后派 GPU 作业。
- 实现定向微调修复代码：新增 mfuzz/neurons/finetune.py（机制层）与 scripts/run_repair_finetune.py（编排）。finetune.py 提供 subnet_conv_weights/subnet_target（按模块路径收责任子网或单层的 Conv2d 权重）、set_trainable、weight_norm_scale（学习率按权重范数归一化）、loc_loss（1-IoU）、HeadLogits（torchvision 半前向停在 NMS 前 head 输出）+ recover_cls_logit（cls 失效回找 NMS 前 anchor 行与 logit 向量）、cls_loss（推正类压错类的 BCEWithLogits）、pick_random_control / pick_bottom_control（两条对照层选层）。脚本扫步数×学习率网格、三条配置各扫一遍、落 result.json 与代价收益前沿图。设计稿第十节五个待定项已改写为已锁定。本地验证：ruff check / format 全绿；逻辑级 smoke（合成张量 + 假适配器）通过，覆盖损失形状/数值/梯度方向、cls anchor 回找正误两路、选层与可复现、权重范数归一化、SGD 能 step 与权重可恢复、12 格点网格脚手架与前沿图。loc 端到端打通；cls 回找路径机制已通，需服务器上 COCO + 真权重验证回找命中率。未在本地验证：真实 FCOS/RetinaNet 前向、COCO 失效计数、GPU 多步微调收敛（本地无权重无 COCO、不连服务器、不跑 GPU）。
- fcos 全量扫描挂后台后在 sweep 阶段 CUDA OOM 失败，未跑出结果。launcher 脚本 output/det/repair_finetune/run_fcos.sh（loc→cls 串行），日志 output/det/repair_finetune/run_fcos.log，用 GPU 物理卡 1（CUDA_VISIBLE_DEVICES=1），launcher PID 2772507、loc PID 2772512、cls PID 2202904/2203046。未带 bottom 对照：服务器上找不到任何带 aggregate.layer_drilldown 的 fcos 结果（output/det 下无 base/fcos，全盘 grep layer_drilldown 为空），按预案不传 --drilldown，只跑 responsible+random，bottom 跳过（缺 drilldown 来源）。loc 顺利过 setup 进了 sweep：B 集基线计数 {'agree': 1442, 'miss': 62, 'spurious': 185, 'loc': 182, 'cls': 44}，配置层 responsible=head.regression_head、random=backbone.body.layer2.1.conv1。但 baseline 阶段就耗约 18 分钟（12:10→12:28，A=500/B=300 三检测器全量前向），进 sweep 后第一条 responsible 配置在 A 集训练循环里 OOM：train_one_config 调 gf.run → struct_attr.py:85 forward_graph → fcos 整图前向，单进程累计占用 21.59 GiB 把整张 24G 卡吃满（OOM 报告里的 "GPU 0" 是 CUDA_VISIBLE_DEVICES=1 重映射后的本进程视角，物理卡就是 1，同卡另有 tandongli 三个 636 MiB 小进程）。说明 smoke（20+20 图、steps 1,3）规模下不显存的累积问题，到全量多步多格点会把卡撑爆。属设计稿外的内存累积问题，已停下报告、未改代码。cls 是同一 launcher 的第二条命令、loc OOM 退出后自动接着跑，正卡在自己约 18 分钟的 baseline 阶段，进 sweep 后会同样 OOM；尝试 kill cls 进程被 auto 模式守卫拦下，留其自行 OOM 退出。
- 服务器小 smoke 通过（fcos，A=20/B=20，steps 1,3 × lr 1e-3，不带 drilldown 故只跑 responsible+random）。两条命令均端到端跑完无 traceback，result.json 与 frontier.png 都落地。**未改任何代码**。B 集基线计数正常（agree 106 / loc 4 / cls 3 / spurious 14 / miss 4，非全 0 非 NaN）。loc：各格点 loc/agree 计数合理，random 对照每格点精确回到基线（权重恢复正确）。cls：HeadLogits 半前向（model.head / anchor_generator / box_coder.decode / FCOS 分数公式 sqrt(sigmoid·sigmoid)）全链跑通无 API 不匹配；recover_cls_logit 在真 COCO 上命中，"训练实例累计 n_inst" > 0（responsible s1=2 s3=39，random s1=2 s3=6）。观察：cls responsible s1 单步整子网微调扰动很大（cls 3→23、agree 106→58），s3 回落到 cls=3 agree=66，属真模型行为非代码 bug；random 配 cls 损失 agree 恒 106（小 backbone 扰动不改 NMS 后检测，符合预期）。耗时粗估：20+20 图、2 格点×2 配置约 1 分钟级（loc ~22s 主循环、cls ~38s）；主成本是每步在 A 集重跑前向重判差分（与图数、步数线性）。
- 更正上一条"可放心跑全量"：全量首跑 OOM（损失图跨 A 集累加）→ 已改梯度累加修复。根因经读代码确认：train_one_config 的训练循环把 A 集全部图的 `loc_loss` / `cls_loss` 累加进一个 `step_loss` 张量，整个 A 集遍历完才调一次 `.backward()`，`step_loss` 一直引用着每张图前向（gf.run 的 GraphResult、cls 还有 HeadLogits.run）的完整计算图，等于把 500 张图的前向激活图同时压在显存里，单进程累计 ~21.6 GiB 把 24 GB 卡撑爆；`del g` 无效（step_loss 仍引用 g 的图）。smoke 只 20 图、累加不到 OOM 量，所以没暴露。修复改成梯度累加：每张图算本图全部失效实例的损失之和 `img_loss`，有失效（k>0）就立刻 `img_loss.backward()` 释放本图计算图，用 `n_terms` 累计本步总实例数，遍历完 A 集后把累计梯度按 `n_terms` 归一（`p.grad /= n_terms`）再 `opt.step()`，与原 `(step_loss / n_terms).backward()` 数学等价、显存被限制在单图量级；loc/cls 两支同改，cls 的 cls_logits/boxes_orig/scores 与 g 每图 backward 后 del。run_baseline / judge_counts 本就在 `@torch.no_grad()` 的 detect 下跑、不动。只改了 scripts/run_repair_finetune.py，finetune.py 未动。本地 ruff check/format 全绿；逻辑 smoke（合成张量+假适配器）确认逐图 backward + 梯度归一与"全损失求和再 backward"逐元素等价（单步/多步/含空图各档），方向、step、实例计数均正确。
- 服务器小规模显存检查通过（fcos，A=100/B=50，steps 1,3,10,30 × lr 1e-3，不带 drilldown 只 responsible+random），单独验 loc 与 cls。先 kill 掉上一次 OOM 留下的整条 run_fcos.sh 进程链（loc 已 OOM 自退、cls 半正卡在 baseline 同样会 OOM），释放显存后开跑。结果：两条命令均无 OOM，峰值显存随 A 遍历与步数稳定不增长——loc 进程峰值约 3.7 GiB（含 ~1.9 GiB 同卡他人小进程，扣掉约 1.8 GiB 是本进程），跑满到 s10/s30 仍恒定；cls 进程峰值约 3.8 GiB（多一条 HeadLogits 图，略高于 loc，同样恒定）。两者都远低于 24 GB、不再 OOM。证明修复把显存限制在单图量级、不随 A 集规模或步数累积，可放心跑全量（A=500）。
- fcos 定向微调全量（A=500/B=300，修复后）已挂后台跑起来，健康越过原 OOM 点、进了 sweep。先用 pwsh sync_lab.ps1 push -Apply 把最新代码推到服务器（确认 run_repair_finetune.py 是梯度累加修复版本到位），未 uv sync。挂在物理卡 GPU 2（CUDA_VISIBLE_DEVICES=2，挑它因只有 ~1.9 GiB 他人小进程、无 repair 作业），launcher 脚本 output/det/repair_finetune/run_fcos_full.sh（loc→cls 串行，全默认格点 steps 1,3,10,30 × lr 1e-4,1e-3,1e-2），日志 output/det/repair_finetune/run_fcos_full.log。launcher bash PID 2458268、loc worker PID 2458372。不带 --drilldown，bottom 自动跳过（服务器仍无 base/fcos 的 layer_drilldown，按预案只跑 responsible+random）。健康确认：模型加载、A 集 500/B 集 300 互不相交、B 集基线计数 {'agree': 1442, 'miss': 62, 'spurious': 185, 'loc': 182, 'cls': 44}（与旧首跑一致，确定性）、配置层 responsible=head.regression_head / random=backbone.body.layer2.1.conv1，并已跑出 sweep 头两个格点 `[responsible] s1_lr0.0001: loc=181 agree=1444 (训练实例累计 223)`、`[responsible] s1_lr0.001: loc=181 agree=1442`——越过原来在第一条训练循环里 OOM 的那个坎、无 OutOfMemory。本进程显存稳定在 3710 MiB（单图量级、不随 A/步数爬向 24 GB），与 A=100 检查一致，修复在全量下成立。baseline 阶段本轮约 4 分钟（GPU 2 较空）。当前只确认进 sweep 即返回，未等 loc/cls 跑完。注：A=100 显存检查的两条进程（loc 635828、cls 3427490，各占 GPU 1/3）当时仍在跑自己单 lr 的 sweep、不碰 GPU 2，kill 它们被 auto 守卫拦下，遂换空闲卡避让、不影响全量。预计 loc+cls 全量耗时数小时（每格点主成本是在 A=500 上重跑三检测器前向重判差分，与图数、步数线性，2 配置×4 步档×3 lr=24 格点/支）。过几小时查进度/拉结果：pwsh -File scripts/sync_lab.ps1 pull -Apply。产物落 output/det/repair_finetune/{loc_fcos,cls_fcos}/data/。
- regen 省着版产物再生已挂后台（恢复"生成失效 + 归因"这条修复线被清掉的产物）。配置 configs/det/regen.toml（extends base，标定集从 train2017 全量缩到 3000 张、目标聚焦 fcos+retinanet、faster_rcnn 仍作差分投票者加载，其余沿用 base：max_iterations=50、PGD、差分、归因、生成失效归因都跑）。先 pwsh sync_lab.ps1 -Apply 把 regen.toml 推到服务器、未 uv sync。挂在物理卡 GPU 0（CUDA_VISIBLE_DEVICES=0；四张卡当时都只有 tandongli 的 pia 小进程各约 636 MiB、每卡 ~1.9 GiB，无我方进程，挑了 util 相对最低的卡 0），入口 nohup bash -c 'PYTHONPATH=. uv run python scripts/run_fuzz.py --config configs/det/regen.toml'，日志 output/det/regen/run_regen.log，python 进程 PID 2975624。健康确认：模型加载、3000 张缩标定集生效（缓存键带图片数，新写 det_fcos_train2017_n3000.pt、不命中 n118287 旧缓存）、两遍流式标定（区间统计 + 频率统计各 3000）跑完无 OOM、fcos 构建完成（种子 194、单元 39328、关键 19679），已进 fuzzing 循环并产失效：轮 0 CNCov 0.839 新失效 20、轮 10 CNCov 0.870 新失效 16、轮 20 CNCov 0.913 新失效 7，迭代推进、覆盖上涨、失效持续产出，本进程显存稳定约 2.3 GiB。只确认越过标定进循环在产失效即返回，未等 max_iterations=50 跑完。fcos（标定+循环+分析）约 1 小时才落 output/det/regen/fcos/data/result.json（含生成失效 + aggregate.layer_drilldown），随后跑 retinanet；整体约两小时。过会儿查进度/拉结果：pwsh -File scripts/sync_lab.ps1 pull -Apply（pull 白名单含 output*）。

## 指针

- 框架：docs/paper_plan/定位与修复验证框架.md（验证逻辑、四类状态、cls 负向证据）
- 设计稿：docs/paper_plan/定向微调试点实验设计.md（框架第三步落地：loc/cls 定向微调试点的责任子网、对照、损失、协议、判据、能力盘点）
- 代码：mfuzz/neurons/struct_attr.py, repair.py, finetune.py
- 脚本：scripts/run_repair_pilot.py, run_repair_grad.py, run_repair_finetune.py
- 文档：docs/paper_plan/Day6/E8修复验证试点.md
- 产物：output/det/repair_pilot, output/det/repair_grad
- 服务器：实验只在实验服务器运行，地址见项目 CLAUDE.md 的 Lab server 一节
