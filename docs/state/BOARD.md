# BOARD

本文件是多会话状态的总入口。仓库保存真实记录，会话只负责执行。需要后续会话继续使用的信息，都写入状态文件。调度规则见 `调度规范.md`。

## 状态写入时机

状态只记录已经发生、可以观测的动作。不要等到会话结束再保存信息，因为会话可能随时中断。

- worker 完成任务后，更新对应工作流文件。
- 调度者完成派发、检查或合并后，更新自己的调度文件。

## 当前进度

四条工作流的当前状态如下：

| 流 | status | owner |
|----|--------|-------|
| [实验](streams/实验.md) | in-progress | A |
| [写作](streams/写作.md) | idle | none |
| [可视化](streams/可视化.md) | idle | none |
| [审计](streams/审计.md) | in-progress | A |

注：Day9 已完成。Day10 修复线做根本转向：从干净 val2017 的跨模型自然分歧，改成在 fuzzing 生成失效上做"产出→归因定位→定向微调修复"的闭环，自然分歧降为负基线；框架文档已重写（docs/paper_plan/定位与修复验证框架.md）。生成失效与归因产物早先被清，正跑 regen 恢复。实验、审计由 A 持有；可视化等真数据、写作挂起，两条均 idle。

实验服务器一次只运行一个 GPU 作业。GPU 队列按照提交顺序执行，作业完成后从队列中移除。

| 作业 | 流 | 提交者 | 状态 | 后台或日志 | 预计 |
|------|----|--------|------|-----------|------|
| （队列空；loc generated 全量已跑完拉回） | | | | | |

loc generated 全量 sweep 跑完并拉回。fcos 干净正例：责任 head.regression_head 修掉约 40% loc（s30/lr0.01 benefit14/cost2，低代价点 benefit4-6/cost≈0），random 与 bottom 全 24 格点 benefit≈0。retina 一致反例：责任头修不动、高 lr 反变差，与 retina loc 归因偏 FPN 一致。合起来"修复跟着归因走"。按用户意愿，这条保留为弱的、接近本质的假设——闭环方式（fuzz 产缺陷→归因定位→定向微调修复）有效——不复核、不升 [结论]，有意保持弱叙述（不声称统计强度与普遍性，N 偏小）。产物 output/det/repair_finetune/generated_loc_{fcos,retinanet}/data/{result.json,frontier.png}。cls 归因不可分且稀，定负向案例、不 densify。验证逻辑见 docs/paper_plan/定位与修复验证框架.md。

活跃调度者：

| 标签 | 负责的工作流 | 调度文件 |
|------|----------|----------|
| A | 实验、写作、可视化、审计 | [调度-A.md](调度-A.md) |

## 工作区

每条工作流使用一个长期复用的 jj 工作区。工作区位于主检出之外，保存对应工作流的成果。共享数据通过符号链接从主检出接入。

| 流 | 工作区 |
|----|--------|
| 实验 | `F:/doc/安全缺陷/工作区/实验` |
| 写作 | `F:/doc/安全缺陷/工作区/写作` |
| 可视化 | `F:/doc/安全缺陷/工作区/可视化` |
| 审计 | `F:/doc/安全缺陷/工作区/审计` |

工作区遵循以下约定：

- 新建工作区后，先运行引导操作，把 `output`、`references`、`datasets` 和 `.venv` 符号链接到主检出。这四个名字已经写入 `.gitignore`，jj 不跟踪这些链接。（output 重整后，检测产物根从 `output_det` 改为统一的 `output`，链接名随之更新。）
- 坑：git-bash 下用 `ln -s` 建目录符号链接，如果没有开 `MSYS=winsymlinks:nativestrict`，会回落成递归复制（看似成功，实为各建一份拷贝）。建链接时要设这个环境变量，建完用 `readlink` 确认是真链接。
- `sync_lab.ps1` 的 pull / pull-env 自动把 `output` 落到主检出（`Get-MainCheckoutRoot`），从任意工作区跑都不覆盖 output 符号链接；push 仍从调用工作区推源码。
- 快照、提交、rebase 等 jj 操作，都在对应工作区目录内执行。
- 调度者一般只在主检出中修改自己的调度文件，不在主检出中修改各工作流的状态文件，当需要时，应该在对应工作区中修改。各工作流各自在自身工作区里修改工作流状态文件，且仅能修改自己的状态文件。这样可以避免不同工作流之间的状态冲突。
- 实验需要把源码同步到服务器时，从各自的工作区运行 `sync_lab.ps1`。脚本按相对路径执行，不受工作区位置影响。
- 各工作流提交自己的结果（worker 在自己工作区提交，调度者亲自做某条流时以该流身份提交）；调度者本身不提交。合并进 linux 只由调度者执行，且每次合并前向用户声明、经同意后进行合并。规则见 `调度规范.md`。

## 工作流文件格式

每条工作流对应一个文件，位置是 `streams/<名字>.md`。文件应包含接手所需的信息。新的会话读取该文件和其中指向的产物后，应该可以继续处理这条工作流。

工作流文件的头部字段如下：

| 字段 | 含义 | 取值 |
|------|------|------|
| `stream` | 工作流名字 | 与文件名一致 |
| `owner` | 当前负责人 | 负责人标签；空闲写 `none` |
| `status` | 进度 | `idle` / `in-progress` / `blocked` / `done` |

正文小节按照以下顺序排列：目标、已知上下文、作业队列与下一步、阻塞、记录、指针。每条记录单独写一行，说明操作、结果和产物位置。状态文件不写日期与时间戳，时间线以提交历史为准；docs/ 下的文档同样不写日期。状态文件只承载对后续工作有用的上下文：把不适合进入上下文的内容——过程流水、一次性操作细节、可从代码或提交历史得到的叙述、与正式文档或他处重复的内容——去掉，只保留决定、当前事实、结论与假设、指针。清理按"适不适合做上下文"来筛，不是按时间删。完整历史在提交记录里查。"已知上下文"的写法见下一节。

## 已知上下文

每条工作流的"已知上下文"用于保存当前有效、但不能直接从代码中读出的信息。worker 接手任务时，需要先读取这一节。

"记录"保存做过的关键操作，"已知上下文"保存当前认识。两者都只承载适合作为上下文的内容；不适合进入上下文的内容（过程流水、操作细节、可从他处得到或重复的叙述）随时去掉，不论新旧。完整历史在提交记录里，不在状态文件里重复保留。

每条上下文写成一行，并带一个类型标签：

| 标签 | 内容 |
|------|------|
| 决定 | 已经确定的选择及其理由 |
| 方法 | 为了保持前后一致，需要继续遵守的协议 |
| 设定 | 关键参数值 |
| 结论 | 已经验证为真的发现，需要带"已复核"标记 |
| 假设 | 暂定、仍待验证的判断 |
| 坑 | 操作陷阱和容易出错的地方 |

上下文条目按以下方式维护：

- 新增：发现一条当前有效、且不能直接从代码中读出的信息时，写入"已知上下文"。
- 升级：假设经过验证后，可以升级为结论。结论升级需要复核，规则见下文。
- 删除：条目被推翻、被取代、受底层代码变化影响，或者已经转入正式文档或代码时，从"已知上下文"删除，并在"记录"里说明删除内容和删除原因。
- 转入正式位置：结论稳定到应该进入正式文档或代码时，移动到对应位置。"已知上下文"只保留指向正式位置的指针。

"已知上下文"只保存对应工作流专有的信息。跨工作流的项目级决定放在路线图或 memory 中；需要引用时写出指针，不要重复记录。

结论升级需要两次确认：

- 产出结论的 worker 完成第一次确认，独立复核的审查者完成第二次确认。没有通过第二次确认的内容，一直保留为假设。
- 审查者需要独立核查当前产物，必要时重新运行相关步骤。审查者不能只阅读叙述后表示同意。worker 和审查者必须是不同的 agent。
- 默认审查者是调度者。后续工作会依赖的重要结论，需要再安排一个冷启动的审查 worker 复核，并在提交汇报中告诉用户。删除结论时也使用同样规则。
- 复核结果需要写入"记录"。"已知上下文"中的结论只标记"已复核"，不附带证据。

## 认领与释放工作流

认领一条工作流：

1. 读取目标工作流的 `owner`。
2. 如果 `owner` 不是 `none`，说明已有会话在处理这条工作流，应当换一条。
3. 如果 `owner` 是 `none`，把 `owner` 改成认领者标签，并把 `status` 改成 `in-progress`。

释放一条工作流：

1. 检查"记录"和"已知上下文"是否完整。
2. 把 `owner` 改回 `none`，并把 `status` 改成 `idle`。

> `owner` 是约定标记，不是锁。它用于避免两个会话同时认领同一条工作流。工具不会强制执行这个约定，所以认领前必须主动检查；如果已经被占用，就换一条。
