# 研究方案修改规划

## 写作原则

1. **叙述主语归位**：技术章节的主语是本项目，不是参考工作。参考工作以脚注式引用出现（"沿用 CriticalFuzz 的思路""参考了 NSGen 的做法"），不用整段介绍别人做了什么。
2. **研究方案 ≠ 文献综述**：每段的逻辑是"本项目做了什么设计 → 这个设计有什么参考依据"，而不是"参考工作做了什么 → 本项目在此基础上扩展"。
3. **三个核心问题**：每个技术节应回答"做什么""为什么这么做""凭什么相信能做成"，第二个是区分好坏的关键。
4. **诚实边界**：每个模块写明局限，比假装完美更有说服力。
5. **风格**：像研究者在解释自己的工作。短句为主，有主语，不堆砌形容词，不照搬审稿意见原话。

## 各章节字数与改动要点

| 章节 | 原始字数 | 当前字数 | 目标字数 | 状态 |
|------|---------|---------|--------|------|
| 总体方案 | ~800 | 1272 | 1200 | 已完成 |
| 2.1（含 2.1.1-2.1.2） | ~1000 | 2136 | 1900 | 待扩充 |
| 2.2（含 2.2.1-2.2.4） | 2534 | 4872 | 5000 | 已完成 |
| 2.3（含 2.3.1-2.3.3） | 2190 | 3751 | 4200 | 已完成 |
| 第 3 节（含 3.1-3.3） | 1358 | 1358 | 2700 | 待修改 |
| **合计** | | **~13389** | **~15000** |

### 总体方案

- 从宏观概述改为有范围限定、技术闭环、创新点和风险边界的完整叙述
- 经历 4 轮重写确立写作风格：前两轮过于碎片化像 checklist，第三轮过度压缩充满 AI 味，第四轮找到了研究者自然叙述的语感
- 最后一轮删去了不适合出现在方案中的公式和实现细节

### 2.1 差分行为驱动

- 核心改动是补全差分测试的技术前提和判定逻辑：oracle 前提条件（同任务、同标签空间、种子预筛选）、严格缺陷判定规则（目标偏离 + 参考保持 = 候选缺陷）、PGD 式迭代过程
- 原文把分歧信号写得过于确定（"直接转化为缺陷触发信号"），修改后明确分歧只是候选信号，需要后续验证
- 修正了若干事实错误（梯度下降→上升、正确类别→共识类别）

### 2.2 中间层关键神经元识别

- 内容层面：以 CriticalFuzz 的频率定义和干预实验为基础，引入 NSGen 的归因贡献作为第二维度，构建本项目的融合关键度指标
- 补充了阈值对关键集合规模的影响、CNCov/CCCov 双指标的设计理由（全局覆盖掩盖类别不均衡）、联合梯度的耦合动机和冲突处理、缺陷分类的激活指纹方案
- 写作层面：发现参考内容占比过重（整段在讲 CriticalFuzz/NSGen 做了什么），做了叙述主语翻转——将参考工作从段落主角压缩为本项目叙述中的脚注式证据。这一原则（"主语归位"）写入了上方的写作原则，后续章节同样适用

### 2.3 融合语义约束

- 核心修正：原文把两种"语义"混为一谈。区分了输入语义保持（相似度要高，防止无效噪声）和决策路径差异（相似度要低，探索不同内部路径）。原文将相似度低解释为"语义漂移"并触发回退，但 NSGen 的 DPD 恰恰保留低相似度样本
- 区分了本项目的轻量路线（关键神经元激活向量，可微，参与梯度优化）和 NSGen 的重管线（自然语言描述 + CLIP 文本相似度，不可微，只能做后验筛选）。原文直接等同二者
- 明确了白盒梯度生成是本项目的创新（继承 DeepXplore 形式），不是 NSGen 的做法。NSGen 使用 95 种变异规则，不是梯度优化
- 能量调度参考关系修正：CriticalFuzz 提供能量调度思想，NSGen 提供路径差异保留准则，本项目融合二者
- 原文只有 $S_{input}$ 一个相似度公式且方向用反。补充了 $S_{path}$ 的形式化定义（基于关键神经元激活向量 $u(\mathbf{x})$），与 $S_{input}$（基于中间层通用特征 $v(\mathbf{x})$）明确区分来源
- 补充了阈值校准方法（NSGen 下四分位数，含各数据集参考阈值）、消融实验设计和局限说明
- 原 2.3.3 白盒定向变异中的"联合修正算子"内容与 2.1.2 和第 3 节重叠，已整合为"语义约束与联合优化的接口"
- 清理了 2.2 和 2.3 中对第 3 节的前向引用（原 4 处），各节内容自包含，不再预告后文细节
- 替换了全部宣传性用词（"确保""极高""坚实语义基础""本质缺陷""高效诱捕"等）

## 第 3 节修改前的待处理事项

- 第 3 节内部仍有两处引用旧 2.3.3 的"联合修正算子 $\mathcal{T}(\cdot)$"和 PGD 迭代公式，需修正或重写（2.2 和 2.3 中的前向引用已清理）
- $obj_{sem}$ 已在 2.3 中明确为 $1 - S_{input}$（输入保持），$S_{path}$（路径差异）走种子筛选。第 3 节的 $obj_{total}$ 说明需与此对齐
- 旧 2.3.3 的白盒定向变异内容（步长、修正算子、领域约束）已从 2.3 移除，与 2.1.2 和第 3 节重叠，需在第 3 节改写时决定归属和粒度
- 第 3 节需要合并到第 2 节成为 2.4，会影响全文编号和交叉引用，具体方案待审稿意见确定
